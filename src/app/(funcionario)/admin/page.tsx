import { requireUser } from "@/lib/auth"; import { listOccurrences } from "@/features/occurrences/_data-access/occurrences"; import { OccurrenceList } from "@/components/occurrence-list"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { categories, categoryLabels, statuses, statusLabels } from "@/lib/domain";
export default async function AdminPage() { await requireUser("staff"); const result = await listOccurrences({ page: 1 }); const open = result.items.filter((item) => item.status === "open" || item.status === "under_analysis").length; const resolved = result.items.filter((item) => item.status === "resolved").length; return <div className="flex flex-col gap-8"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-primary">Administração</p><h1 className="mt-2 font-heading text-4xl font-bold">Gestão de ocorrências</h1><p className="mt-2 text-muted-foreground">Acompanhe e gerencie as solicitações do condomínio.</p></div><div className="grid gap-4 md:grid-cols-3"><Card><CardHeader><CardTitle className="text-sm font-medium text-muted-foreground">Total de ocorrências</CardTitle></CardHeader><CardContent className="text-4xl font-bold text-primary">{result.total}</CardContent></Card><Card><CardHeader><CardTitle className="text-sm font-medium text-muted-foreground">Abertas e em análise</CardTitle></CardHeader><CardContent className="text-4xl font-bold text-amber-700">{open}</CardContent></Card><Card><CardHeader><CardTitle className="text-sm font-medium text-muted-foreground">Resolvidas nesta página</CardTitle></CardHeader><CardContent className="text-4xl font-bold text-emerald-700">{resolved}</CardContent></Card></div><form className="grid gap-3 rounded-xl border bg-card p-4 md:grid-cols-[1fr_1fr_auto]"><select name="status" className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todos os status</option>{statuses.map((status) => <option key={status} value={status}>{statusLabels[status]}</option>)}</select><select name="category" className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todas as categorias</option>{categories.map((category) => <option key={category} value={category}>{categoryLabels[category]}</option>)}</select><button className="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">Filtrar</button></form><OccurrenceList items={result.items} total={result.total} basePath="/admin/ocorrencias" /></div>; }
