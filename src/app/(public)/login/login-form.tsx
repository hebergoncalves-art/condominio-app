"use client";
import Link from "next/link"; import { useActionState } from "react"; import { requestMagicLinkAction } from "../_actions/auth"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export function LoginForm() { const [state, action, pending] = useActionState(requestMagicLinkAction, undefined); return <Card><CardHeader><CardTitle>Seu email</CardTitle></CardHeader><CardContent><form action={action} className="flex flex-col gap-4"><div><Label htmlFor="email">Email cadastrado</Label><Input id="email" name="email" type="email" required className="mt-2" /></div>{state && !state.success && <p className="text-sm text-destructive">{state.message}</p>}{state?.success && <p className="text-sm text-emerald-700">{state.message}</p>}<Button type="submit" disabled={pending}>{pending ? "Enviando…" : "Enviar link mágico"}</Button><Link href="/cadastro" className="text-center text-sm text-muted-foreground underline">Ainda não tenho cadastro</Link></form></CardContent></Card>; }
