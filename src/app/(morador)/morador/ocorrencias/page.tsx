import Link from "next/link"; import { Plus } from "lucide-react"; import { requireUser } from "@/lib/auth"; import { listOccurrences } from "@/features/occurrences/_data-access/occurrences"; import { occurrenceFilterSchema } from "@/lib/validations"; import { categories, categoryLabels, statuses, statusLabels } from "@/lib/domain"; import { OccurrenceList } from "@/components/occurrence-list"; import { Button } from "@/components/ui/button"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
export default async function ResidentOccurrencesPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { await requireUser("resident"); const raw = await searchParams; const parsed = occurrenceFilterSchema.safeParse({ status: typeof raw.status === "string" ? raw.status : undefined, category: typeof raw.category === "string" ? raw.category : undefined, page: raw.page }); const filters = parsed.success ? parsed.data : { page: 1 }; const result = await listOccurrences(filters); return <div className="flex flex-col gap-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-primary">Área do morador</p><h1 className="mt-2 font-heading text-4xl font-bold">Ocorrências</h1><p className="mt-2 text-muted-foreground">Acompanhe os registros do condomínio.</p></div><Link href="/morador/ocorrencias/nova"><Button><Plus data-icon="inline-start" /> Nova ocorrência</Button></Link></div><form className="grid gap-3 rounded-xl border bg-card p-4 md:grid-cols-[1fr_1fr_auto]"><select name="status" defaultValue={filters.status ?? ""} className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todos os status</option>{statuses.map((status) => <option key={status} value={status}>{statusLabels[status]}</option>)}</select><select name="category" defaultValue={filters.category ?? ""} className="h-10 rounded-md border bg-background px-3 text-sm"><option value="">Todas as categorias</option>{categories.map((category) => <option key={category} value={category}>{categoryLabels[category]}</option>)}</select><Button type="submit" variant="secondary">Filtrar</Button></form><OccurrenceList items={result.items} total={result.total} basePath="/morador/ocorrencias" /></div>; }
