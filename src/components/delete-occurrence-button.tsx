"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { deleteOccurrenceAction } from "@/app/(morador)/morador/ocorrencias/_actions/occurrences"; import { Button } from "@/components/ui/button";
export function DeleteOccurrenceButton({ id, redirectTo = "/morador/ocorrencias" }: { id: string; redirectTo?: string }) { const router = useRouter(); const [pending, setPending] = useState(false); const remove = async () => { if (!window.confirm("Excluir esta ocorrência?")) return; setPending(true); const result = await deleteOccurrenceAction(id); setPending(false); if (result.success) router.push(redirectTo); }; return <Button variant="destructive" onClick={remove} disabled={pending}>{pending ? "Excluindo…" : "Excluir ocorrência"}</Button>; }
