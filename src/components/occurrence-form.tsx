"use client";
import { useState } from "react"; import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { occurrenceSchema } from "@/lib/validations"; import { categories, categoryLabels } from "@/lib/domain"; import { Button } from "@/components/ui/button"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { Label } from "@/components/ui/label"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"; import type { z } from "zod";
type Values = z.infer<typeof occurrenceSchema>;
export function OccurrenceForm({ action, initial, id }: { action: (data: FormData) => Promise<any>; initial?: Partial<Values>; id?: string }) { const [message, setMessage] = useState<string>(); const form = useForm<Values>({ resolver: zodResolver(occurrenceSchema), defaultValues: { title: initial?.title ?? "", description: initial?.description ?? "", category: initial?.category ?? "maintenance", location: initial?.location ?? "" } }); const submit = async (values: Values) => { setMessage(undefined); const data = new FormData(); Object.entries(values).forEach(([key, value]) => data.set(key, String(value))); if (id) data.set("id", id); const photo = (document.getElementById("photo") as HTMLInputElement | null)?.files?.[0]; if (photo) data.set("photo", photo); const remove = (document.getElementById("removePhoto") as HTMLInputElement | null)?.checked; if (remove) data.set("removePhoto", "on"); const result = await action(data); setMessage(result.message); if (result.success) form.reset(values); }; return <Card><CardHeader><CardTitle>Dados da ocorrência</CardTitle></CardHeader><CardContent><form onSubmit={form.handleSubmit(submit)} className="flex flex-col gap-5"><div><Label htmlFor="title">Título</Label><Input id="title" {...form.register("title")} className="mt-2" />{form.formState.errors.title && <p className="mt-1 text-sm text-destructive">{form.formState.errors.title.message}</p>}</div><div><Label htmlFor="description">Descrição</Label><Textarea id="description" {...form.register("description")} className="mt-2 min-h-32" />{form.formState.errors.description && <p className="mt-1 text-sm text-destructive">{form.formState.errors.description.message}</p>}</div><div><Label>Categoria</Label><Select value={form.watch("category")} onValueChange={(value) => form.setValue("category", value as Values["category"], { shouldValidate: true })}><SelectTrigger className="mt-2"><SelectValue /></SelectTrigger><SelectContent>{categories.map((category) => <SelectItem key={category} value={category}>{categoryLabels[category]}</SelectItem>)}</SelectContent></Select></div><div><Label htmlFor="location">Local</Label><Input id="location" {...form.register("location")} placeholder="Ex.: Torre A, área da piscina" className="mt-2" />{form.formState.errors.location && <p className="mt-1 text-sm text-destructive">{form.formState.errors.location.message}</p>}</div><div><Label htmlFor="photo">Foto (opcional, JPG/PNG até 5 MB)</Label><Input id="photo" name="photo" type="file" accept="image/jpeg,image/png" className="mt-2" />{id && <label className="mt-2 flex items-center gap-2 text-sm text-muted-foreground"><input id="removePhoto" type="checkbox" /> Remover foto atual</label>}</div>{message && <p className="text-sm text-muted-foreground">{message}</p>}<Button disabled={form.formState.isSubmitting} type="submit">{form.formState.isSubmitting ? "Salvando…" : id ? "Salvar alterações" : "Salvar ocorrência"}</Button></form></CardContent></Card>; }
