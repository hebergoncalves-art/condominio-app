---
applyTo: "src/**/*.tsx"
---

Component rules:

- Prefer Server Components.
- Add `"use client"` only when necessary.
- Keep components small and focused.
- Page-specific components belong in `_components`.
- Shared components belong in `src/components`.
- Use TypeScript.
- Use Tailwind CSS.
- Prefer Shadcn UI for standard UI elements.